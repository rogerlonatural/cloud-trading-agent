[pytest]
testpaths = tests
markers =
    live: hits real Fubon Neo API (requires FUBON_LIVE_LOGIN=1 + cert config)
# Default: exclude live tests so CI / local unit runs never need the real SDK.
addopts = -m "not live"
